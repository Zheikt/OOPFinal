package edu.neumont.oop.Model;

public class Ship {
    private String type;
    private int size;
    private int[][] boardLocations; //Do we want to store each individual location or just the starting location and direction vector (e.g. x=0, y=1)
}
